{
  "name": "debut",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite src",
    "prepare-files": "mkdir dist && cp -R ./src/* ./dist/ && find ./dist -name '*.css' -exec rm {} +",
    "buildcss": "tailwindcss -i ./src/styles.css -o ./dist/style.css --minify",
    "build": "npm run prepare-files && npm run buildcss",
    "watch": "tailwindcss -i ./src/styles.css -o ./src/style.css --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.5.1",
    "tailwindcss": "^3.4.17",
    "vite": "^6.0.7"
  }
}
